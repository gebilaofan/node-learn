// 路由统一处理
// export = (router) => {

//   require("./auth")(router);

//   // router.get("/", (req, res) => res.status(200).json({ message: "Welcome to the TODO API. Check the documentation for the list of available endpoints" }));

// };
